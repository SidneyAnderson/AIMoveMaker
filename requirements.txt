# AI Movie Maker - Production Dependencies
# All versions pinned per PRD requirement

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Database
sqlalchemy[asyncio]==2.0.36
alembic==1.14.1
aiosqlite==0.20.0
asyncpg==0.30.0

# Task Queue
celery[redis]==5.4.0
redis==5.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
authlib==1.4.0
httpx==0.28.1

# Email
fastapi-mail==1.4.2

# Logging
loguru==0.7.3

# Validation & Serialization
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# AI Pipelines
# NOTE: torch, torchvision, torchaudio, xformers are installed by setup.sh
#       from the PyTorch CUDA index (not PyPI) so they are NOT listed here.
# NOTE: audiocraft and TTS are installed by setup.sh with --no-deps because
#       they pin torch==2.1.0 / numpy==1.22 which conflict with our CUDA build.
diffusers==0.33.1
transformers==4.46.3
accelerate==1.2.1
safetensors==0.6.2
controlnet-aux==0.0.9
opencv-python-headless==4.10.0.84
numpy>=1.24.0,<2.0

# File Processing
Pillow==11.1.0
ffmpeg-python==0.2.0

# External Integrations
huggingface-hub==0.27.1

# Utilities
python-dotenv==1.0.1
